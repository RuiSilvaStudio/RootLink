---
type: Note
_width: wide
---

# UI Build Backlog — roles/permissions gaps

> **Status:** Backlog — nothing here has been built yet, except two items
> closed post-Phase-6 by product decision rather than by building UI (see
> `docs/roles-permissions/phase0-decisions.md` Addendum 5): the
> professional-entity promote/demote row and the entity-registration-vs-
> conversion-ambiguity row, both under P1 below, now read "RESOLVED."
> **Companion doc:** `ACTION_UI_MAP.md` (what exists today, backward-looking).
> This doc is forward-looking: for every action with no UI (or a degraded
> one), what should actually get built, why it matters, roughly how big the
> lift is, and what it depends on.

## How this backlog is organized

Grouped into four priority bands, reasoned as follows:

1. **P0 — Security & account-integrity controls.** Anything where a
   compromised, abusive, or misbehaving account currently has **no admin
   lever at all** in the UI, even though the backend can already stop them.
   These are the actions most likely to matter in an actual incident
   (account takeover, harassment, spam) — a missing button here means "wait
   for someone to hand-write a `curl`/DB query," which is the worst place to
   discover a gap.
2. **P1 — Delegable authority & entity-management parity.** Actions where
   the *design* explicitly wants an entity (or its super admin) to self-serve
   something the backend supports, but the UI silently forces it back to
   "type the URL by hand" or "ask platform staff." These aren't
   security-critical the way P0 is, but they're the difference between the
   permission system actually working as designed vs. being decorative for
   these specific rows.
3. **P2 — Content-workflow completeness.** Degraded UI (edit-less comments,
   collapsed review/approve) and missing archive controls for
   events/products — real product gaps, lower urgency than account safety.
4. **P3 — Platform admin/settings & polish.** Settings key-value store,
   entity-scoped notifications, i18n backfill, and product-decision
   ambiguities that don't block anyone today but should be resolved before
   they cause confusion later.

Backend-only or backend+UI items are called out explicitly in each row and
summarized again in the closing note — don't schedule them as pure frontend
tickets.

---

## Backlog table

| Action | Why it matters | Suggested UI | Complexity | Depends on |
|---|---|---|---|---|
| **P0 — Security & account-integrity controls** |
| `user.restrict_suspend_ban_lift` (entity) + `user.restrict_suspend_ban_lift_platform_wide` (platform) | The entire 4-rung enforcement ladder (§4 — Active/Restricted/Suspended/Banned) exists end-to-end in the backend (Phase 4: `/api/admin/users/{id}/restrict`, `/lift-restriction`, `/suspend`, `/lift-suspension`, `/ban`, `/unban`, with GDPR tombstone anonymization on ban) but has **zero admin-panel controls**. Today the only way to restrict, suspend, or ban a misbehaving user is a direct API call or DB edit — e.g. a suspended trusted-publisher's status can only be set that way, not through any admin screen. | Add a rung-status action group to `/admin/users/page.tsx`'s per-row actions column, next to the existing key-icon reset-password button: a status `<select>` or button group (Active / Restrict / Suspend [+ expiry date picker] / Ban) with a reason-text confirm dialog (matching the `prompt()`/`confirm()` pattern already used for reset-password), plus a status Badge per row so restricted/suspended/banned users are visibly flagged in the list. The platform-wide vs. entity-scoped split can share the same component; which endpoint fires depends on whether the row is inside `/admin/users` (platform) or a future entity-scoped roster view. | Medium — no new backend work, but it's a genuinely new interaction (reason capture, expiry-date input for suspensions, status badge, and wiring 6 distinct endpoints instead of 1). | None blocking — purely additive frontend work against already-shipped Phase 4 endpoints. |
| `session.revoke_own` | Backend (`POST /api/auth-security/sessions/revoke-mine`) exists; there's no "log out of all devices" control anywhere, so a user who suspects their account is compromised has no self-service way to kill other sessions short of a password reset (which itself has no UI either — see below). | A "Log out of all other devices" button on the account/security settings page (wherever password/profile settings live), calling the existing endpoint directly. | Small — one button + confirm dialog + one existing endpoint. | None. |
| `session.revoke_other` | Backend (`POST /api/auth-security/sessions/{user_id}/revoke`) exists but isn't called from anywhere — not even the admin ban/suspend flow, so banning or suspending someone today doesn't actually force their existing session to expire; they can keep using a live token until it naturally times out. | Wire this into the restrict/suspend/ban action above on `/admin/users/page.tsx` (auto-revoke on suspend/ban) **and** expose a standalone "Force logout" button per row for the admin+ / entity-super-admin case where you want to kill a session without also changing status. | Small once the restrict/suspend/ban UI above exists (same row, same confirm-dialog pattern) — Medium if built standalone first. | Best sequenced right after the restrict/suspend/ban item above, since suspend/ban without a forced logout is a half-finished safety control. |
| `password.reset_own` | The self-service "forgot password" backend flow (`/api/auth-security/password/reset/request`, `/confirm`) exists but there's no "Forgot password?" link on `/auth/login` or anywhere else — every password reset today has to go through a platform admin using the platform-wide reset tool. | Add a "Forgot password?" link on `/auth/login` leading to a new request/confirm form (email → token → new password), reusing `/auth/login`'s existing form styling. | Medium — new 2-step flow (request + confirm), though the backend contract is already fully built and tested. | **Needs product/dev sign-off that dev-only token-in-response-body behavior is acceptable to ship as-is** — no email-sending infrastructure exists in this codebase yet (confirmed, no SMTP/provider integration anywhere per `phase0-decisions.md` Addendum 2), so the request endpoint currently returns the raw reset token directly in the JSON response as an explicitly-flagged dev-only stand-in. Shipping this UI before real email delivery exists means the reset "link" would have to be manually copied from a network response — acceptable for internal/admin use, not for real end users. |
| `password.reset_entity_member` | An entity's own super admin (or a delegate, per §10) has no way to reset a member's password without escalating to platform staff — the only reset control that exists at all is the platform-wide admin one. | Add a key-icon "Reset password" button to `/entity/[entityId]/team/page.tsx`'s members table (same interaction as `/admin/users/page.tsx`'s existing key icon — prompt for new password, confirm, call the entity-scoped endpoint), gated on `isOwnSuperAdmin` or a `password.reset_entity_member` delegation grant. | Small — same UI pattern already proven on `/admin/users/page.tsx`, just a new endpoint target and a delegation-aware gate. | **Backend gap:** per `ACTION_UI_MAP.md` row, no entity-scoped reset-password endpoint currently exists — this needs a small backend addition (`POST /api/entities/{id}/members/{user_id}/reset-password` or similar) before the button has anything to call. |
| `trusted_publisher.grant_revoke_platform_default` (platform) | The admin grant/revoke endpoint (`PATCH /api/admin/users/{id}/self-publish`) exists server-side; `lib/api.ts`'s `selfPublish` client only wires the end-user's own eligibility-check/accept flow, never the admin-facing grant/revoke — so an admin who wants to manually grant or pull "trusted publisher" status (e.g. after a policy violation, or to fast-track a known-good contributor) has no click path. | Add a "Trusted publisher" toggle/badge + Grant/Revoke button to `/admin/users/page.tsx`'s per-row actions, next to Verify/Unverify (same icon-button style, `CheckCircle`/`XCircle` pairing already used for verification). | Small — one new column/button pair calling an already-shipped endpoint. | None blocking. |
| `trusted_publisher.grant_revoke_entity` | §7 gives an entity's own admin/super-admin the ability to grant/revoke trusted-publisher status *within their entity* (doesn't scale if every grant needs platform staff) — but no entity-scoped implementation exists under this name at all, so today this authority is 100% centralized on the platform admin panel regardless of entity size. | Add the same Grant/Revoke control to `/entity/[entityId]/team/page.tsx`'s members table, gated on `isOwnSuperAdmin`/admin+ within the entity. | Medium — needs the backend endpoint first (see Depends on), then a UI nearly identical to the platform-wide row above. | **Backend gap:** no entity-scoped grant/revoke endpoint or model exists under this action name — confirmed by grep. Needs a new `POST /api/entities/{id}/members/{user_id}/trusted-publisher` (or similar) endpoint before any button can call it. |
| **P1 — Delegable authority & entity-management parity** |
| `product.manage_any` | The backend already lets an entity's moderator+ edit/delete another user's marketplace listing (`PUT`/`DELETE /api/marketplace/listings/{id}`) — but `/marketplace/[id]` only ever renders Edit/Delete when `isOwner` is true. A moderator/admin has to already know and type `/marketplace/edit/{id}` by hand; there is no discoverable click path, so the delegable "manage any product" tier from §10 is effectively unusable in practice. | On `/marketplace/[id]`, render the existing Edit/Delete buttons whenever `isOwner || can("product.manage_any")` (mirroring the pattern already used correctly on `/learning/courses/[id]` for `course.manage_any` — "one of the cleanest Phase 3 wire-ups" per `ACTION_UI_MAP.md`). Optionally add a small "Managing as moderator" indicator so it's clear this isn't the owner acting. | Small — the backend and even the edit page already work; this is a client-side render-condition fix, not new functionality. | None — this is the most template-ready item in the whole backlog; copy `course.manage_any`'s existing `usePermission()` wiring directly. |
| `event.manage_any` | Same shape of gap as `product.manage_any`: `_check_event_owner` in `app/api/events.py` already lets moderator+ update/delete any event, but `/events/[id]` gates every edit/delete/venue/amenity/schedule control on `isOwner` only. The page's own `isAdmin` variable is used solely for ticket check-in, never for event management, so a moderator with real backend authority has no button to use it. | Same fix pattern as above: render `/events/[id]`'s Edit/Delete/venue/amenity/schedule controls when `isOwner || can("event.manage_any")`, reusing the existing `isAdmin` variable or extending it. | Small — same class of fix as `product.manage_any`, slightly larger surface area (multiple sub-sections: venue, amenities, schedule). | None. |
| `group.manage_any` — parity check | `ACTION_UI_MAP.md` reports this one *does* have a working `canManage` gate on `/groups/[id]`'s Edit icon. No action needed here — listed only so the product owner sees the pattern applied correctly at least once, to use as the template for the two rows above. | N/A — already built. | N/A | N/A |
| ~~Entity-scoped promote/demote for `professional` entities~~ — **RESOLVED, by design, not a gap** | ~~`phase0-decisions.md` Addendum 3 found that `professional` entities have no shared "team" schema...~~ **Resolved post-Phase-6 (`phase0-decisions.md` Addendum 5): the product owner confirmed professional entities never get entity-scoped promote/demote capability, permanently — this is organization-only by design, not a temporary gap awaiting a schema decision.** Professional-kind users' rank changes are handled directly via `/admin/users` instead. Verified (not newly built): `role_requests.py`'s existing block is correct and permanent; professional accounts never get an `entity_id`, so `/entity/[entityId]/team` is structurally unreachable for them (confirmed by `tests/test_roles_decisions_professional_no_team_workflow.py`); no dangling UI link was found offering this path. `ROLES_PERMISSIONS.md` §3/§6 updated to state this outright rather than describe it as an open gap. | N/A — no schema will be built for this. | N/A | Closed — no further action. |
| Delegation auto-void on demotion | §10 states delegation grants are voided "on any demotion... of the grantee," but `phase0-decisions.md` Addendum 4 confirms this is wired for departure/dissolution only, **not** for demotion (`role_requests._decide`) — a demoted user can retain a delegated permission (e.g. `article.review`) they may no longer be appropriate to hold, with no UI surfacing that this happened or letting a super admin manually clean it up in the meantime. | Until the underlying rule is decided, add a lightweight "stale?" indicator to `/entity/[entityId]/team/page.tsx`'s delegations list — flag (visually, e.g. an amber Badge) any active delegation whose grantee's current rank is now below the delegated action's registry `min_rank`, so the entity super admin can spot and manually revoke it via the existing Revoke button (already built). This doesn't require the underlying auto-void logic to be finished — it's a stopgap visibility fix using data the page already fetches (`registry`, `delegations`, `members`). | Small — pure client-side computation over already-fetched data + one Badge. | **Blocked on a design decision** (per Addendum 4's own framing): should a grant survive a *partial* demotion that still leaves the grantee above the action's own `min_rank`, or does ANY demotion void it regardless? The stopgap above works either way, but the real auto-void fix needs this decided first. |
| `platform_ui.edit_content` delegation-gate mismatch | The registry marks this delegable all the way down to persona(1) (§10's broadest delegation reach of any action) — but the floating "Edit page" toggle is hard-coded to `role === "super_admin"` only in `editor-mode-provider.tsx` (the component's own comment says so explicitly). A delegated persona/contributor/moderator/admin — e.g. a "Copy editor" badge holder per §2 — gets a delegation grant through the UI that then does nothing; the button never appears for them. | Update `editor-mode-provider.tsx`'s visibility check to also honor an active `platform_ui.edit_content` delegation grant for the current user (the same delegation data already fetched/available via `usePermission()`/the registry), not just `role === "super_admin"`. | Small–Medium — one conditional in an existing provider, but needs the delegation-lookup wired into a context that currently only checks static role. | None blocking, but should be sequenced with or after any delegation-lookup helper work, to avoid duplicating that logic. |
| ~~Entity-registration vs. entity-conversion ambiguity~~ — **RESOLVED: conversion is self-service only, as designed** | ~~`phase0-decisions.md` Addendum 4 flags that a `professional` user can become an organization's founder through two different, un-reconciled flows...~~ **Resolved post-Phase-6 (`phase0-decisions.md` Addendum 5): the product owner confirmed entity conversion is intentionally self-service only** — no admin-triggered path, no `user_id` parameter anywhere in `entity_conversion.py`/its request schemas or endpoints (confirmed by reading the code, and by a dedicated test that a smuggled `user_id` in the request body is ignored). Both flows existing side by side is accepted as intentional: `/entity/convert` is the fast, immediate, self-service path (no review, no `user_id`, always acts on the caller); the general-purpose entity-registration path remains the review-gated route for founding a genuinely new, separate organization. No merge of the two flows was requested or built. | N/A — no flow merge was requested. | N/A | Closed — no further action. If a future session wants explanatory copy distinguishing the two flows for end users (the original "Suggested UI" idea below this row's history), that remains a separate, small, optional polish item, not a blocker. |
| **P2 — Content-workflow completeness** |
| `compost_listing.create_edit_own` + `compost_listing.archive` | Grepping the whole backend for `compost_listing` turns up only the registry's own two entries — there is no model, endpoint, or table under this name at all. The live composting feature is a different model entirely (`/composting` — hub creation, joining, logging deposits), with no per-item create/edit/archive-by-someone-else's-item semantics matching what these two registry actions describe. Right now these are two registry rows describing a feature that doesn't exist, not just a missing button. | **No UI can be meaningfully scoped until a product decision is made** (see Depends on). If the decision is "yes, build it": a per-user compost-listing create/edit form + list, modeled on `/marketplace/create`/`/marketplace/[id]`'s existing owner-only edit/delete pattern, plus a super-admin archive control on a new admin page modeled on `/admin/groups/page.tsx`'s archive button. | **Large** — full new feature (model, endpoints, list/detail/edit pages, admin archive page), not a small addition. | **Blocked on a product decision, and needs backend work first regardless of the decision's outcome**: does the platform actually want a distinct "compost listing" (an individual's item-for-exchange, marketplace-style) separate from the existing hub/deposit model, or should these two registry entries simply be retired as superseded by `/composting`? Don't schedule frontend work here until that's answered — building UI for a feature that turns out to be redundant with `/composting` would be wasted effort. |
| `comment.add_edit_remove_own` — missing "edit" | Registry name says "add/edit/remove," but only add (`POST`) and remove (`DELETE`) exist — there's no `PATCH` comment endpoint and no edit UI in `CommentSection.tsx`. A user who makes a typo in a comment has no fix short of delete-and-repost (losing thread position/replies). | Add an inline edit mode to `CommentSection.tsx` (pencil icon next to the existing trash icon, on your own comments only) that swaps the comment text for an editable field + Save/Cancel, calling a new `PATCH` endpoint. | Medium — needs a new backend endpoint plus the inline-edit UI (no existing pattern in this component to copy from; nearest analog is the article editor's Save flow, but comments are inline, not a separate page). | **Backend gap:** no `PATCH /api/comments/{id}` (or equivalent) endpoint exists — this is backend work, not just frontend, same as the compost-listing and event-archive items below. |
| `article.review` vs. `article.approve` — collapsed into one step | §7 models these as two distinct actions with different rank floors (review at contributor+, approve at moderator+) and the registry documents review as delegable down to contributor separately from approve — but in the live UI both collapse into the exact same "Approve"/"Unreview" buttons on `/admin/review-queue` and `/admin/content`. There is no separate "mark reviewed, awaiting approval" intermediate state a contributor can produce that a moderator then approves — it's a single moderator-gated step in practice (with a documented enforcement mismatch letting contributor+ through, per `ACTION_UI_MAP.md`). | If the product owner wants the two-step design for real: add a genuine intermediate `in_review` → `reviewed` → `approved` status, with a contributor-facing "Mark reviewed" button distinct from a moderator-facing "Approve" button on `/admin/review-queue`/`/admin/content`. If the current one-step behavior is actually fine (simpler, works today), explicitly re-scope the registry/spec to match reality instead of carrying a permanent documented mismatch. | **Large** if building the real two-step flow (new status value, new endpoint, new button, content-state migration); **Small** if the decision is instead to update the docs to match the simpler shipped behavior. | **Blocked on a product decision**: is the two-step review/approve distinction actually wanted, or was collapsing it into one step the right call that the spec should be updated to reflect? Don't build the Large option without that answer first. |
| `event.archive` (platform) | No `/admin/events` page exists, no `archiveEvent` client method, and no super-admin-gated archive endpoint distinct from the existing owner/moderator+ `DELETE /api/events/{id}`. Platform staff have no way to soft-archive a problematic event across entities (e.g. one with attendees from multiple orgs) without hard-deleting it via the owner/moderator delete path. | Build a new `/admin/events` page, modeled directly on `/admin/groups/page.tsx`'s archive pattern (search/list table + super-admin-gated "Archive" button per row, same `isSuperAdmin` client check + confirm dialog + toast). | **Medium** for the frontend (new page, but a near-exact copy of an existing, working pattern) — **plus required backend work** (see Depends on). | **Backend gap:** no distinct "archive" (soft) endpoint exists for events today — only the existing hard `DELETE`. Needs a new `POST /api/admin/events/{id}/archive` (soft-archive, matching `group.archive`'s "hidden not deleted" semantics) before the page has anything real to call. |
| `notification.send_to_entity_members` | The only notification-broadcast endpoint (`POST /api/admin/broadcast`) sends to literally every user platform-wide — there's no entity-scoped variant, so an organization's own admin/super-admin has no way to message just their own members (e.g. "the AGM has been rescheduled") without spamming the entire platform or resorting to manual direct messages one at a time. | Add a "Notify my members" form to `/entity/[entityId]/team/page.tsx` (or the entity's own dashboard page), modeled on `/admin/notifications/page.tsx`'s existing Broadcast textarea + send button, but scoped to `entity.members` instead of all users. | **Medium** for the frontend (near-identical to the existing broadcast form) — **plus required backend work** (see Depends on). | **Backend gap:** no entity-scoped broadcast endpoint exists at all — needs a new `POST /api/entities/{id}/notify-members` (or similar), audience = that entity's member list, before any button can call it. |
| **P3 — Platform admin/settings & polish** |
| `platform.manage_settings_taxonomy` — generic key-value Settings store half | `/admin/config` fully covers the taxonomy-families half of this action (add/edit/delete families and categories) — but the separate, real generic key-value Settings store (`api.admin.listSettings`/`getSetting`/`updateSetting`, backed by `GET/PUT /api/admin/settings[/{key}]`) has **no UI at all**; nothing in the frontend calls it, so any platform-wide config value meant to live in this store today can only be read/written via direct API calls. | Add a new tab or sibling section on `/admin/config` (keep it visually adjacent to the taxonomy tree, since it's the same registry action) — a simple key/value list-and-edit table: list all settings (`listSettings()`), inline edit value + description per row (click-to-edit, matching `/admin/config`'s existing inline-edit-field pattern for family rows), Save calling `updateSetting`. | **Large** — no existing key-value editor pattern anywhere in the codebase to copy verbatim; needs its own list/edit/validate UI, and settings values may be typed (string/number/bool/JSON) with no existing schema-driven form generator in this codebase to reuse. | None blocking (endpoints are fully built) — but worth scoping down first (does every setting need a rich editor, or is a plain textarea-per-key acceptable for v1?) before committing to the Large estimate. |
| Phase 5 UI surfaces — missing i18n | `phase0-decisions.md` Addendum 4 flags that all ~7 new Phase 5 pages (entity registration, `/entity/convert`, `/entity/[entityId]` dissolution/ban section, `/entity/[entityId]/team`, the verification review queue, and related forms) use hardcoded English copy instead of the `t()`/`messages/{locale}.json` pattern every other page in this codebase follows. Not broken (the `t()` fallback returns the raw key, nothing crashes) but these pages are the only ones on the platform not localized, which will read as an inconsistency to any non-English user who reaches them. | Extract the hardcoded strings from those ~7 pages into `messages/en.json`/`messages/pt.json` following the existing `t("namespace.key")` convention used everywhere else, and swap the JSX over. | Medium — mechanical but touches every string on 7 pages; no design work needed, just following the existing i18n pattern. | None — purely additive, can be done incrementally page-by-page. |

---

## Items intentionally NOT included here

- **Permission bundles** (§10's "Roadmap note") — explicitly deferred to a
  future iteration in the spec itself, not a current gap. Not scheduled here;
  revisit only if delegation-sprawl actually becomes a pain point.
- **Enforcement-floor mismatches** (e.g. `article.approve`/`plants.create_edit`/
  `plants.import`/`link.remove_submitted` enforcing a looser rank than the
  registry states) — these are backend authorization bugs, not missing UI.
  They belong on a backend tech-debt list (see `TECH_DEBT.md`), not this one.

---

## Backend work required, not just frontend

Three groups of items above need real backend work before a UI has anything
to call — flagging these clearly so they aren't scheduled as pure frontend
tickets:

1. **`compost_listing.create_edit_own` / `compost_listing.archive`** — no
   backend endpoint or model exists under this name at all. Before any UI
   backlog item makes sense here, the product owner needs to decide: is this
   a real, distinct feature from the existing composting-hub/deposit model
   (`/composting`), and if so, what does a "compost listing" actually contain
   that a hub deposit doesn't? If the answer is "no, hubs/deposits already
   cover this," the two registry entries should be retired instead of built
   toward.
2. **`comment.add_edit_remove_own`'s missing edit**, **`event.archive`**, and
   **`notification.send_to_entity_members`** — each needs a new backend
   endpoint (`PATCH` comment, soft-archive event, entity-scoped broadcast,
   respectively) before its corresponding UI row above can be built. These
   are called out individually in the table's Depends on column too.
3. **`trusted_publisher.grant_revoke_entity`** and
   **`password.reset_entity_member`** — both need small new entity-scoped
   endpoints (grant/revoke self-publish, reset password) mirroring
   already-shipped platform-wide equivalents; low backend risk (the platform
   version is a working template to copy), but still not pure frontend work.
